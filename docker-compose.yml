version: '3.8'

services:
  ai-devops-platform:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - /app/__pycache__
    environment:
      - PYTHONPATH=/app
    env_file:
      - .env
    restart: unless-stopped